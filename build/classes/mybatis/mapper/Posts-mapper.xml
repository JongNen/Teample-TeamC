<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="messages">
	
	<insert id="write" parameterType="Hashmap">
		INSERT INTO REVIEWS(POSTNUM, WRITER, IMG, TITLE, POSTBODY) 
				VALUES(REVIEWS_SEQ.NEXTVAL, #{writer}, #{image} , #{title}, #{postBody})
	</insert>
	<update id="modify" parameterType="Hashmap">
		UPDATE REVIEWS
			SET	 TITLE = ${title}
			 	 IMG = ${image}
			 	 POSTBODY=${postBody} 		
		WHERE WRITER=#{writer}
	</update>
	<delete id="delete">
		DELETE WRITER WHERE ID={writer}
	</delete>
</mapper>